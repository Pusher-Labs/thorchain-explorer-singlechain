export interface PoolMember {
    asset: string;
    rune_address: string;
    asset_address: string;
    last_stake: string;
    last_unstake: string;
    units: string;
    pending_rune: string;
    pending_tx_id: string;
}
