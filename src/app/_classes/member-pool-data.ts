export class MemberPoolData {
    asset: string;
    units: string;
    dateFirstStaked: number; // will change to dateFirstDeposited
    heightLastStaked: number; // will change to heightLastDeposited
}
