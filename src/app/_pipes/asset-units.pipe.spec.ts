import { AssetUnitsPipe } from './asset-units.pipe';

describe('AssetUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
